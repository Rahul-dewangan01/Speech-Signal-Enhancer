# ─── Core dependencies ───────────────────────────────────────────────────────
numpy>=1.23.0
scipy>=1.9.0
matplotlib>=3.6.0
soundfile>=0.12.0
pandas>=1.5.0

# ─── Deep Learning ───────────────────────────────────────────────────────────
torch>=2.0.0

# ─── Speech Quality Metrics (optional — code handles missing gracefully) ─────
# pesq requires C compiler (Microsoft C++ Build Tools on Windows).
# Install manually:  pip install pesq
# pystoi is pure Python and installs easily.
pystoi>=0.3.3

# ─── Optional ────────────────────────────────────────────────────────────────
# librosa>=0.10.0      # uncomment for MP3/M4A/AAC input support
# pesq>=0.0.4          # uncomment if you have C++ Build Tools installed
